<tr>
    <td>
        {% if level == 1 %}
            {{ typeOffre.name }}
        {% endif %}
    </td>
    <td>
        {% if level == 2 %}
            {{ typeOffre.name }}
        {% endif %}
    </td>
    <td>
        {% include '@AcMarchePivot/typeoffre/_flags.html.twig' %}
    </td>
    <td>
        {% if typeOffre.urn %}
            <a href="{{ path('pivot_typeoffre_show', {'id': typeOffre.id}) }}" title="Détails">
                {{ typeOffre.urn }}
            </a>
        {% endif %}
    </td>
    <td>
        {{ typeOffre.typeId }}
    </td>
    <td>
        {{ typeOffre.type }}
    </td>
</tr>
