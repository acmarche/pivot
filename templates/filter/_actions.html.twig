<div class="d-print-none">
    <a href="{{ path('pivot_filtre_edit', { 'id': filter.id }) }}" role="button"
       class="btn btn-primary"><i class="fas fa-edit"></i> Traduire</a>

    <a href="{{ path('pivot_filtre_index') }}" role="button" class="btn btn-primary"><i class="fas fa-list"></i> Retour
        à la liste</a>
    <br/><br/>
</div>
